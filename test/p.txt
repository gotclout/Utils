Generate a system architecture diagram describing ClaimMind. ClaimMind is a unified service that consolidates        document and claim processing behind a shared foundation. Include the following components

  -- Azure AI Foundry Hub as governance management plane. It should be named ClaimMind.
     -- This component should contain the following:
        -- Model Catalog
           -- GPT Versions
           -- Embeddings
           -- Model Inventory
        -- Content Safety
           -- Prompt shields
           -- Jailbreak detection
           -- Content Filters
        -- Evaluations
           -- Quality Metrics
           -- RAG
           -- Safety Score
           -- Accuracy Metrics
        -- Agent Registry
           -- Agent Identity
           -- Agent Capabilities/Tools
           -- Owner
           -- Classification (data access privileges/agent roles)

        -- Control Plane
           -- Includes Foundry Inference Endpoint
           -- Dashboard for monitoring and registering alerts
           -- Drift detection

  -- AKS deployment as control plane
     -- 2 Pod deployment with replicas
        -- Pod output logged as structured JSON via DataDog Agent
     -- Include Scaling Policy
     -- Include Routing/Ingress Configuration
     -- Using Namespaces:
        -- agent-dev, agent-staging, agent-production
     -- Combined FastAPI LangGraph Agent
     -- CI/CD pipeline using GitHub Actions from build test push
     -- Helm deployment
     -- Foundry Agent RBAC Roles

  -- Data plane consisiting of the following managed/unmanaged services
    -- Azure Postgres with pgvector extension
    -- FalkorDB
    -- Azure Key Vault (Secrets Management)
    -- Data Dog
       -- Dashboard Monitoring & Alerting
       -- Includes structured output logged via datadog agent in control plane
